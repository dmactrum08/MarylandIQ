# MarylandIQ — Python dependencies
# Install with: pip install -r requirements.txt
# Python 3.11+ required.

# ---------------------------------------------------------------------------
# Core
# ---------------------------------------------------------------------------
python-dotenv>=1.0.0          # Load .env variables
requests>=2.31.0              # HTTP requests (scraping fast path)
beautifulsoup4>=4.12.0        # HTML parsing
lxml>=4.9.0                   # Fast HTML/XML parser backend for BeautifulSoup

# ---------------------------------------------------------------------------
# Supabase
# ---------------------------------------------------------------------------
supabase>=2.3.0               # Supabase Python SDK

# ---------------------------------------------------------------------------
# Playwright (Playwright fallback for JS-rendered candidate websites)
# After pip install, also run: playwright install chromium
# ---------------------------------------------------------------------------
playwright>=1.41.0

# ---------------------------------------------------------------------------
# Google AI Studio / Gemini (Stage 2+)
# ---------------------------------------------------------------------------
google-generativeai>=0.4.0

# ---------------------------------------------------------------------------
# Content extraction
# ---------------------------------------------------------------------------
trafilatura>=1.6.0            # Main content extraction (strips nav/footer/ads)
                              # Used by discover_thin_candidates.py (Phase 3)

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
tenacity>=8.2.0               # Retry logic for network requests
httpx>=0.27.0                 # Async HTTP (used by supabase SDK — pin to avoid StreamReset issues)
